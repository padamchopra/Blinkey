<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#efefef"
    android:focusable="true"
    android:focusableInTouchMode="true"
    tools:context="es.esy.practikality.blinkey.MainActivity">
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:text="Manual Recognition"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="25sp"
        android:textAllCaps="false"
        android:textStyle="bold"
        android:fontFamily="casual"
        android:onClick="response"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="casual"
        android:textAlignment="center"
        android:background="@color/colorAccent"
        android:hint="Speech Recognised\nor\nContent to Speak"
        android:textColorHint="#adadad"
        android:textColor="@color/colorPrimaryDark"
        android:height="290dp"
        android:scrollbars="vertical"
        android:id="@+id/stringtospeak"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:focusable="true"
        android:focusableInTouchMode="true">
        <Button
            android:onClick="SpeechInput"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/colorPrimaryDark"
            android:text="Start"
            android:textAlignment="center"
            android:gravity="bottom"
            android:textColor="@color/colorPrimary"
            android:textStyle="bold"
            android:textSize="80sp"
            android:fontFamily="sans-serif-condensed"
            android:id="@+id/speakbutton"/>
        <Button
            android:onClick="SpeechInput"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/colorPrimaryDark"
            android:text="Listening"
            android:gravity="top"
            android:textAlignment="center"
            android:textStyle="bold"
            android:textSize="45sp"
            android:fontFamily="sans-serif-condensed"
            android:id="@+id/speakbuttonit"/>
    </LinearLayout>
</LinearLayout>